import math #math is a package built into python

numeros = [1, 2, 3, 4, 5, 6]

pi = math.pi
#golden ratio \/
golden = (pi * ((abs(-((pow(4, 4)) + 4)) + (max(numeros) * math.sqrt(4))) / (math.ceil(6.1) * math.floor(3.9))) * ((min(numeros)) / pi))

#pi constant returns pi
#abs returns absolute (positive) value of number
#max returns highest number in a list
#min returns lowest number in a list
#sqrt returns square root of number
#ceil returns number rounded up
#floor returns number rounded down
#pow requires 2 inputs, number and then power e.g. pow(4, 3) = 64 because its 4^3